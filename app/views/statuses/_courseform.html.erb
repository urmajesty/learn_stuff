<%= f.label :course_id, "Select a Course:" %>
    <%= f.collection_select :course_id, Course.all, :id, :title, include_blank: true    %>

    

  <%= f.fields_for :course do |c| %>



  <%= c.label :rating %>
  <%= c.text_field :rating %>
    <br></br>


  <%= c.hidden_field :learner_id, value: current_learner.id %>
    <br></br>

  <% end %>